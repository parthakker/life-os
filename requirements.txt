# Core dependencies
python-telegram-bot==21.0
anthropic==0.71.0

# Vector embeddings via OpenAI API
openai>=1.55.3  # Latest version, fixes httpx proxies error
numpy==2.0.2  # Still needed for cosine similarity

# Database
# SQLite is built into Python - no installation needed
psycopg[binary]>=3.2  # Modern PostgreSQL adapter with Python 3.13 support

# Optional: If you want to use Flask API (api_server.py)
flask==3.1.0
flask-cors==5.0.0
